# SCons / MSVC build artifacts
*.obj
*.os
*.o
*.exp
*.ilk
*.pdb
*.lib
.sconsign.dblite
*.log
# vendored import libs ARE tracked (needed to build); allow them back:
!external/lib/*.lib
# SCons VariantDir, as in every sibling extension.
Temp/
